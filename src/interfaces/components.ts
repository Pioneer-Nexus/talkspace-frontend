export enum ButtonType {
  PRIMARY = 'primary',
  DEFAULT = 'default',
  TEXT = 'text',
  LINK = 'link',
}

export enum ButtonSize {
  LARGE = 'large',
  DEFAULT = 'default',
  SMALL = 'small',
}

export enum ButtonIconPosition {
  START = 'start',
  END = 'end',
}
